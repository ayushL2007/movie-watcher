{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // Replace with your entry file (e.g., main.py if that's your Flask app)
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"  // Matches the src above
    }
  ]
}